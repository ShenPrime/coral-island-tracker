/**
 * Shared constants for the frontend application.
 * Kept separate to avoid circular import issues.
 */

// Key for storing pending "What's New" flag in localStorage
// Set when version mismatch is detected, cleared after showing the banner
export const PENDING_WHATS_NEW_KEY = "coral-tracker-pending-whats-new";
